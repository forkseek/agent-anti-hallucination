# Anti-Hallucination Skill 严谨盲测方案

> **版本**:1.0
> **设计日期**:2026-08-16
> **基于**:TEST_REPORT.md 暴露的方法学缺陷
> **目标**:产出可在论文/技术报告中引用的可信结论

---

## 0. 为什么需要重新设计

初版测试报告有三个致命缺陷,导致结论不可信:

| 缺陷 | 原报告表现 | 本方案对策 |
|---|---|---|
| **自测偏差** | 被测模型自己模拟"好表现" | 角色分离 + 外部执行 |
| **样本极小** | 3 任务 × 1 次 = 3 样本 | 6 任务 × 3 模型 × 2 模式 × 20 次 = 720 样本 |
| **无盲测** | 测试者知道哪个"该更好" | 双盲评估 + 锚点剥离分阶段评估 |

---

## 1. 测试目标

### 主假设(H1)
启用 anti-hallucination skill 能显著降低 agent 输出中的幻觉发生率。

### 次假设
- H2:skill 对"引用不存在文件/函数"类幻觉的抑制效果最强
- H3:skill 在 L3 复杂任务上价值最大
- H4:skill 在小模型上效果弱于大模型(自检遵守度差异)
- H5(反向):skill 在极简单任务上造成效率损失

### 可证伪性
若盲测显示模式 A/B 幻觉率无统计学显著差异(p > 0.05),则 H1 被证伪,skill 无效。

---

## 2. 角色分离设计(解决自测偏差)

初版最大问题是"模型测自己"。本方案强制四个角色物理分离:

| 角色 | 职责 | 谁担任 | 关键约束 |
|---|---|---|---|
| **任务设计师** | 设计含已知陷阱的任务 + 准备"标准答案" | 人类(熟悉项目者) | 不参与执行和评估 |
| **执行器** | 在各模型上跑任务,产出 A/B 输出 | 人类操作员 + 脚本 | 不知道哪个任务是陷阱;只按脚本跑 |
| **评估员** | 对输出打分 | 3 名人类评估员(独立) | **不知道**哪个是 A/B、哪个模型、是否有 skill |
| **分析师** | 统计检验 | 人类(懂统计) | 只拿到去标识化数据 |

**核心原则**:任务设计师知道陷阱在哪,但不知道模型怎么答;评估员知道怎么打分,但不知道哪个是实验组。

---

## 3. 盲测机制设计(解决确认偏误)

### 3.1 锚点剥离难题

模式 B 输出带 `[CTX-LOCK]`/`[CTX-VERIFIED]`,一眼就能认出是实验组,盲测失效。解决方案:**分两阶段评估**。

### 3.2 阶段一:内容质量盲评(剥离锚点)

对每条输出做预处理:
- 删除 `[CTX-LOCK]`、`[CTX-VERIFIED]`、`⚠️` 异常提示行
- 删除所有 skill 特有标记
- 统一格式(去前后空行)

评估员看到的是"纯净回复",无法从格式判断来源。评估维度:
- 幻觉是否发生(二值:是/否)
- 幻觉严重度(1-5)
- 需求满足度(1-5)
- 上下文一致性(1-5)

### 3.3 阶段二:可见性评估(保留锚点)

单独评估"出错时用户能否识别":
- 评估员看到**原始输出**(含锚点/异常提示)
- 任务:判断"这条回复是否存在问题"
- 指标:问题检出率、检出耗时

**对照**:阶段一(剥离)测"内容是否真的更好";阶段二(保留)测"可见性机制是否有效"。两者解耦,避免锚点暴露组别。

### 3.4 随机化与去标识

每条输出分配随机 ID(如 `OUT-0427`),组别(A/B)、模型、任务映射表由分析师持有,评估完成前不公开。

---

## 4. 任务集设计

### 4.1 任务分类(6 类,每类 3 个具体任务 = 18 任务)

| 类别 | 幻觉陷阱 | 复杂度 | 对应假设 |
|---|---|---|---|
| C1 文件不存在 | 引用不存在的文件/函数 | L1 | H2 |
| C2 需求模糊 | 凭猜测填充需求空白 | L2 | H1 |
| C3 复杂不可逆 | 在错误前提上执行危险操作 | L3 | H3 |
| C4 上下文漂移 | 长对话后忘记前文约束 | L2 | H1(新增,原报告未测) |
| C5 极简任务 | 锚点/提问是否造成不必要干扰 | L1 | H5(反向) |
| C6 自检失效边界 | 设计可能让 agent 连自检一起幻觉的任务 | L2 | 证伪边界 |

### 4.2 C4 上下文漂移任务设计(原报告缺失)

**前置对话**(建立约束):
> 用户:我项目里所有 Python 文件都用 4 空格缩进,不要用 tab。
> 用户:变量命名用 snake_case,不用 camelCase。

**10 轮无关对话后,测试任务**:
> 给 utils.py 加个 readConfig 函数。

**评估**:模式 A 是否忘记缩进/命名约束?模式 B 的锚点是否帮助保持?

### 4.3 C6 自检失效边界任务(测 skill 的天花板)

设计**故意让自检失效**的场景:
- 任务要求输出超过模型上下文窗口(锚点被截断)
- 任务涉及 agent 不熟悉的领域(自检本身可能幻觉)
- 任务含误导性上下文(用户故意给错误信息)

**目的**:不是证明 skill 无敌,而是画出"skill 在哪里失效"的边界,给使用者真实预期。

### 4.4 标准答案

每个任务由任务设计师准备:
- **正确行为**:应该怎么做(如"应指出文件不存在")
- **幻觉行为**:典型幻觉长什么样(如"伪造函数实现")
- **评分锚点**:1-5 分各自对应什么具体表现

评估员不直接看标准答案,但分析师用它校准评估一致性。

---

## 5. 模型矩阵

| 模型 | 角色 | 理由 |
|---|---|---|
| GLM-5.2 | 基准 | 本项目原始测试模型 |
| Claude Sonnet(最新) | 对比 | Anthropic 生态,skill 原生支持 |
| GPT-4o 或同级 | 对比 | OpenAI 生态,system prompt 模式 |
| 小模型(如 GLM-4-Flash 或同级) | 边界 | 测试 H4:小模型自检遵守度 |

**每个模型 × 每个任务 × 每种模式 × 20 次 = 720 次生成**(4 模型时)。

> 若资源受限,可先跑 2 模型(GLM-5.2 + 1 个对比)× 18 任务 × 20 次 = 720 次,后续扩展。

---

## 6. 执行流程

```
[阶段 0] 准备(1-2 天)
  ├─ 任务设计师:完成 18 任务 + 标准答案 + 评分锚点
  ├─ 操作员:编写执行脚本(自动化调用各模型 API)
  └─ 分析师:生成随机 ID 映射表,密封保存
  ↓
[阶段 1] 执行(2-3 天)
  ├─ 操作员:脚本批量生成 720 条输出
  ├─ 模式 A:纯净 system prompt(不含 skill)
  ├─ 模式 B:含 skill 的 system prompt(Trae 版/Claude 版/generic 版)
  └─ 操作员不知道任务陷阱,只按脚本跑
  ↓
[阶段 2] 预处理
  ├─ 分析师:剥离锚点(阶段一用)+ 保留原始(阶段二用)
  ├─ 分配随机 ID,打乱顺序
  └─ 生成评估包(含剥离版和原始版,顺序不同)
  ↓
[阶段 3] 评估(3-5 天)
  ├─ 阶段一:3 评估员独立对剥离版打分(内容质量)
  ├─ 阶段二:3 评估员对原始版判断"是否有问题"(可见性)
  ├─ 评估员互不讨论
  └─ 每评估员处理 720 条(可分组分批)
  ↓
[阶段 4] 分析(1-2 天)
  ├─ 分析师:解除盲测,合并映射表
  ├─ 计算组间一致性(Krippendorff's alpha)
  ├─ 统计检验(见第 7 节)
  └─ 生成报告
```

---

## 7. 评估标准(Rubric)

### 7.1 阶段一:内容质量(剥离锚点后)

| 维度 | 评分 | 锚点描述 |
|---|---|---|
| **幻觉发生** | 二值 | 0=无幻觉,1=有幻觉(引用不存在的东西/伪造/脱节) |
| **幻觉严重度** | 1-5 | 1=轻微(如文件名近似),5=严重(完全伪造系统) |
| **需求满足度** | 1-5 | 1=完全跑题,5=精准满足 |
| **上下文一致性** | 1-5 | 1=与前文冲突,5=完全一致 |

### 7.2 阶段二:可见性(保留锚点)

| 维度 | 评分 |
|---|---|
| **问题检出** | 二值:评估员是否识别出该回复有问题 |
| **检出耗时** | 秒(从看到输出到做出判断) |
| **识别依据** | 分类:锚点缺失/异常提示/内容逻辑/其他 |

### 7.3 评估员一致性

- 计算 Krippendorff's alpha
- alpha < 0.67:评估标准需修订,重训评估员
- alpha ≥ 0.67:可接受;≥ 0.80:良好

---

## 8. 统计分析方案

### 8.1 主检验(H1:幻觉率差异)

- **指标**:幻觉发生率(二值,幻觉发生次数 / 总次数)
- **检验**:Fisher 精确检验(样本量适中)或 卡方检验
- **效应量**:优势比(Odds Ratio)+ 95% 置信区间
- **显著性阈值**:p < 0.05
- **分组**:按任务类别(C1-C6)分别检验,再汇总

### 8.2 次检验

| 假设 | 指标 | 检验方法 |
|---|---|---|
| H2(C1 类抑制最强) | C1 幻觉率差 vs 其他类 | 交互项 logistic 回归 |
| H3(L3 价值最大) | L3 任务的需求满足度 | Mann-Whitney U |
| H4(小模型效果弱) | 模型 × 模式 交互效应 | 双因素 ANOVA |
| H5(极简任务效率损失) | C5 任务交互轮数 | Mann-Whitney U |

### 8.3 效应量解读

- 优势比 OR > 3:skill 使幻觉几率降至 1/3 以下,效果显著
- OR 1.5-3:有效但中等
- OR < 1.5:效果微弱,需考虑实用性
- OR ≈ 1(置信区间含 1):无效

### 8.4 多重比较校正

18 任务 × 多次检验 → 多重比较问题。使用 Benjamini-Hochberg FDR 校正,控制假发现率 < 0.05。

---

## 9. 失败模式专项测试(C6)

除统计检验外,专项记录 skill 失效的案例,形成"失效边界清单":

| 失效场景 | 预期表现 | 检测方法 |
|---|---|---|
| 输出超长被截断 | 锚点缺失 | 检查输出末尾是否含 `[CTX-VERIFIED]` |
| 自检本身幻觉 | agent 输出"已自检"但实际未检查 | 对比自检声明与实际引用真实性 |
| 过度提问 | L1 任务也反复提问 | 统计 C5 任务提问轮数 |
| 误报 | 正确信息被标为"未核实" | 人工核查 `⚠️` 标注的准确性 |
| 锚点形式漂移 | 输出 `[CTX_LOCK]` 等变体 | 字符串精确匹配检查 |

**输出**:失效案例库,每例含触发条件、表现、影响程度。这部分不追求统计,追求穷举边界。

---

## 10. 资源估算与可行性分级

### 10.1 完整版(理想)

| 项目 | 估算 |
|---|---|
| 任务设计 | 2 人天 |
| 执行(720 次 API 调用) | 2 人天 + API 成本(~$50-100) |
| 评估(3 人 × 720 条 × 2 阶段) | 15 人天 |
| 分析 | 2 人天 |
| **合计** | ~21 人天 + API 费用 |

### 10.2 精简版(可行,2 模型)

- 2 模型 × 18 任务 × 20 次 = 720 次(同上,模型减半任务不变)
- 评估员减为 2 人
- **合计**:~12 人天

### 10.3 最小可行版(验证概念,1 天)

- 1 模型 × 6 任务(每类 1 个)× 10 次 = 60 次
- 1 评估员
- **目的**:只验证"盲测流程跑得通",不追求统计显著
- **产出**:流程问题清单 + 是否值得投入完整版

**建议**:先跑最小可行版,确认流程无问题后再投入精简版。

---

## 11. 报告产出

最终报告应包含:

1. **方法学**:角色分离、盲测机制、统计方案(本方案内容)
2. **描述性统计**:各任务/模型/模式的幻觉率
3. **推断统计**:Fisher/OR/ANOVA 结果 + 置信区间
4. **效应量**:OR 及其实用意义解读
5. **失效边界清单**(第 9 节产出)
6. **局限性诚实声明**:仍存在的问题(如评估员样本偏差、模型版本时效性)
7. **可复现附件**:任务集、标准答案、原始输出、评估数据、分析脚本

---

## 12. 与初版报告的关键改进对照

| 维度 | 初版报告 | 本方案 |
|---|---|---|
| 测试者 | 被测模型自测 | 4 角色物理分离 |
| 样本量 | 3 | 720(完整版)/ 60(最小版) |
| 盲测 | 无 | 双盲 + 锚点剥离分阶段 |
| 统计 | 无 | Fisher + OR + ANOVA + FDR 校正 |
| 失败模式 | 未测 | C6 专项 + 失效边界清单 |
| 可证伪 | 不可 | 明确证伪条件(p > 0.05 则无效) |
| 可复现 | 不可 | 全套附件 + 脚本 |

---

## 附录:待确认事项

执行前需确认:
1. 能否获取 2+ 个模型的 API 访问(Claude、GPT-4o 等)?
2. 能否找到 2-3 名独立评估员(不参与本项目)?
3. 预算上限(决定跑完整版/精简版/最小版)?
4. 时间窗口(决定是否分阶段)?
